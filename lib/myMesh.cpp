/*
  myMesh.cpp - Library to handle web server controller related functions.
  Created by Cedric Lor, January 2, 2019.
*/

#include "Arduino.h"
#include "myMesh.h"

myMesh::myMesh(/*int pin*/)
{
  // pinMode(pin, OUTPUT);
  // _pin = pin;
}

void myMesh::dot()
{
  // digitalWrite(_pin, HIGH);
  // delay(250);
  // digitalWrite(_pin, LOW);
  // delay(250);
}

void myMesh::dash()
{
  // digitalWrite(_pin, HIGH);
  // delay(1000);
  // digitalWrite(_pin, LOW);
  // delay(250);
}
